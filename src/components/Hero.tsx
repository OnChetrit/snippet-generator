function Hero() {
  return (
    <section className="hero">
      <p className="eyebrow">VS CODE SNIPPET BUILDER</p>
      <h1>Write the file. Keep the editable parts.</h1>
      <p className="hero-copy">
        Create a file template with editor completions, then turn its changing
        pieces into native VS Code snippet variables and tabstops.
      </p>
    </section>
  );
}

export default Hero;
